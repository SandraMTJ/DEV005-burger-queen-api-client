
import Logo from "../../assets/logo.png";

const LogoLogin = () => {
    return (
        <div>
            <img src={Logo} alt="logo" className="logo-login" />
        </div>
    );
};

export default LogoLogin;
